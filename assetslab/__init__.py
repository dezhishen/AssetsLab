"""AssetsLab — 数据驱动角色素材管线。

Species → Presets → Motions → Render → Export

核心模块：
    types.py     — 数据结构类型声明（TypedDict，对应 interfaces.ts）
    server.py    — HTTP API 服务器
    motion.py    — 动作引擎
    render.py    — 骨架渲染
"""
__version__ = "2.0.0"
