# Overrides pyinstaller-hooks-contrib/stdhooks/hook-workflow.py, which targets
# the PyPI 'workflow' package and is incompatible with our own `workflow` engine
# package. An empty hook means "no special handling" for our workflow package.
