# AssetsLab Python dependencies
#
# Install into the project virtual environment (Linux/macOS):
#   python3 -m venv .venv
#   .venv/bin/python -m pip install -r requirements.txt
#
# Windows:
#   py -m venv .venv
#   .venv\Scripts\python.exe -m pip install -r requirements.txt

Pillow>=10.0
